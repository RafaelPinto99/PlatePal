<%= turbo_frame_tag "plan_recipe_#{plan_recipe.id}" do %>
  <div class="show-recipes d-flex flex-row justify-content-between align-items-center">
    <div class="d-flex justify-content-between align-items-center px-5 mb-5 gap-5">
      <%= cl_image_tag plan_recipe.recipe.photo.key, width: 500, height: 500, crop: :fill, class: "border rounded-circle shadow" %>
      <div class="d-flex flex-column justify-content-evenly gap-5 w-75 me-5">
        <h1 class="m-0 text-center dark-green"><%= plan_recipe.recipe.name %></h1>
        <div class="green-border-button d-flex justify-content-evenly align-items-center text-center gap-4 mx-5" style="width:60vh">
          <p class="m-0"><i class="fa-solid fa-clock"></i> <%= plan_recipe.recipe.cook_time %></p>
          <p class="m-0"><i class="fa-solid fa-fire"></i> <%= plan_recipe.recipe.calories %></p>
          <p class="m-0"><i class="fa-solid fa-person"></i> <%= plan_recipe.recipe.servings %></p>
        </div>
      </div>
    </div>
  </div>
  <div class="show-recipes d-flex flex-row justify-content-center align-items-center">
    <div class="d-flex flex-column justify-content-center align-items-center gap-5">
      <div class="d-flex justify-content-between gap-5 p-5">
        <div class="w-75">
          <h2 class="text-start my-0 pb-2 px-5 text-nowrap">How to Cook</h2>
          <div class="pt-2 mb-5 border-top-orange">
            <p class="d-inline-block m-0 p-1 px-5"><%= plan_recipe.recipe.instructions %></p>
          </div>
        </div>
        <div class="w-25">
          <h2 class="d-inline-block text-center my-0 pb-2 px-5 w-100">Ingredients</h2>
          <div class="pt-2 mb-5 border-top-orange d-flex flex-column justify-content-center">
            <% plan_recipe.recipe.recipe_ingredients.each do |ingredient|%>
              <p class="d-inline-block text-center mb-0 p-1 px-5"><%= ingredient.ingredient.name.capitalize %></p>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>
