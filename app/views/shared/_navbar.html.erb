 <nav class="d-flex justify-content-between align-items-center">
  <%= link_to root_path do %>
    <%= image_tag "image.png", alt: "PlatePal", width: "150px" %>
  <% end %>
  <div class="d-flex flex-row align-items-center">
    <%# <li class="me-3">Home</li> %>
    <%# <li class="mx-3">My Calendar</li>
    <li class="mx-3">Contacts</li>
    <li class="ms-3">About</li> %>
  </div>
  <div class="d-flex align-items-center">
    <%= link_to "Generate Plan", surveys_path, class:"orange-border-button me-3 text-decoration-none"%>
    <div class="dropdown">
      <% if user_signed_in? %>
        <button class="avatar navbar-toggle bg-warning" role="button" data-bs-toggle="dropdown" aria-expanded="false" >
          <i class="fa-solid fa-user text-white"></i>
        </button>
        <ul class="dropdown-menu p-0">
          <li class="dropdown-item py-2 px-4"><%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class:"text-green text-decoration-none"%></li>
        </ul>
      <% else %>
        <%= button_to '<i class="fa-solid fa-user text-white"></i>'.html_safe, new_user_session_path, class:"text-decoration-none avatar navbar-toggle bg-warning"%>
      <% end %>
    </div>
  </div>
</nav>
