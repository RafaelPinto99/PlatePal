<%= simple_form_for(@survey, url: wizard_path, method: :put) do |f| %>
  <%= f.collection_radio_buttons :availability, Survey.availability_options, :first, :last %>

  <%= f.submit 'Next' %>
<% end %>
